#!/bin/sh
# Focus Limiter pre-push hook — full gate before code leaves your machine.
# Runs the entire suite: type check, unit + coverage, build, and e2e.
#
# Breakglass: git push --no-verify
set -e
echo "[pre-push] full suite: type check + unit + e2e (skip with --no-verify)"
npm run typecheck
npm test
