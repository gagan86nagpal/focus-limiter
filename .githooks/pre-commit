#!/bin/sh
# Focus Limiter pre-commit hook — fast gate before every commit.
# Runs the type check and the unit suite (which enforces 100% coverage).
#
# Breakglass: git commit --no-verify
set -e
echo "[pre-commit] type check + unit tests (skip with --no-verify)"
npm run typecheck
npm run test:unit
